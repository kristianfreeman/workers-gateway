{
  "name": "workers-gateway",
  "version": "0.0.1",
  "description": "Connect and invoke Lambda functions from inside Cloudflare Workers, without API Gateway",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kristian Freeman <kristian@kristianfreeman.com>",
  "license": "MIT",
  "dependencies": {
    "aws4": "^1.8.0"
  }
}
